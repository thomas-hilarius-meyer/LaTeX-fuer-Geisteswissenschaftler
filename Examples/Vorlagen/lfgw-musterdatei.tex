% !TEX encoding = UTF-8 Unicode
% !TEX TS-program = pdflatex

\documentclass[headsepline,headinclude,%
12pt,% 10pt, 11pt
a4paper, % a5paper ...
%abstracton,% 
pagesize,%
twoside,%
]{scrartcl} % scrreprt, scrbook

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[ngerman]{babel}

%
% Schriftauswahl
%
%\usepackage{lmodern}
%\usepackage{tgadventor}		% Avant Garde 			*sans* font
%\renewcommand{\rmdefault}{qag} % als Standard font verwenden
%\usepackage{tgbonum}			% Bookman 				*serif* font
%\usepackage{tgchorus}			% Zapf Chancery 		*serif* font
%\usepackage{tgcursor}			% Courier 				*tt* font
%\renewcommand{\rmdefault}{qcr} % als Standard font verwenden (nicht wirklich)
%\usepackage{tgheros}			% Helvetica 			*sans* font
%\renewcommand{\rmdefault}{qhv} % als Standard font verwenden
%\usepackage{tgpagella}			% Palatino 				*serif* font
%\usepackage{tgtermes}			% Times 				*serif* font
%\usepackage{tgschola}			% Century Schoolbook 	*serif* font
%\usepackage{iwona}				% Iwona					*serif* font
%\usepackage{anttor}			% Antykwa Torunska		*serif* font
%
% Schriften aus dem Buch
%
\usepackage{libertine}
\usepackage[supstfm=libertinesups,%
   supscaled=1.2,%
   raised=-.13em]% match XHeight of libertine
   {superiors}
\usepackage[scaled=0.8]{beramono}
%
% Einheiten konsistent darstellen
%
\usepackage{xfrac,siunitx}
\sisetup{%
    tight-spacing=true, %
    %math-rm=\mathsf, 
    %		text-rm=\sffamily,
    detect-all, %Zahlen werden in der aktuellen Schrift angezeigt
    detect-family,
    exponent-to-prefix  	= true,%
    round-mode          	= places,% 
    round-precision     	= 2, %
    %  zero-decimal-to-integer,  %aus "2.0" wird "2"
    group-minimum-digits 	= 4, % Für "Tausenderpunkt" --> 1.234 anstatt 1234
    group-separator			={.},% für "12.345" statt "12 345"
    %  scientific-notation = engineering, % Use multiples of 3 as exponent
    locale					=DE, % Typeset numbers and units the German way
    %  range-phrase 			={$\times$},%
    range-units				=single,  % --> 2 x 2 m, - auskommentieren für 2 m x 2 m
    %  quotient-mode=fraction,% 	slash für inline Brüche a/b
    %  per-mode=fraction,%			Bruch statt ^-1
    %  fraction-function=\sfrac % Schönere Brüche
    %  unit-color=myred,
}
% \num{12345,67890}
% \si{\meter\squared}
% \si{\kilogram\per\meter\cubed}
% \SI{1.23}{\joule\per\mol\per\kelvin}
% \numrange[range-phrase = {\,--\,}]{10}{20} => 10 -- 20
% \numrange[range-phrase = { bis }]{10}{20} => 10 bis 20
% \numrange[range-phrase = { $\times$}]{10}{20} => 10 x 20
% \SIrange{0.13}{0.67}{\milli\metre}

%\usepackage[a4,center,cross]{crop}

%\usepackage{imakeidx}
%\indexsetup{level= \subsection*, toclevel = subsection, noclearpage}

%\makeindex[               title = Allgemeiner Index]
%\makeindex[name = person, title = Personenverzeichnis]
%\makeindex[name = bibel,  title = Verzeichnis der Bibelstellen]

%
% Aktive Links im Inhaltsverzeichnis, Index, usw.
% PDF-Metadaten
%
\usepackage{cmap,hyperxmp,hyperref}	% Immer zuletzt laden
\hypersetup{					% setup the hyperref-package options
	pdftitle={LaTeX für Geisteswissenschaftler},	% 	- title (PDF meta)
	pdfsubject={Handbuch},% 	- subject (PDF meta)
	pdfauthor={varia},	% 	- author (PDF meta)
	pdfauthortitle={},
	pdfcopyright={Copyright (c) \the\year\. All rights reserved.},
	pdfhighlight=/N,
	pdfdisplaydoctitle=true,
	pdfdate={\the\year-\the\month-\the\day}
	pdflang={de},
	pdfcaptionwriter={varia},
	pdfkeywords={{LaTeX}, {Geisteswissenschaften}},
%	pdfproducer={XeLaTeX},
	pdflicenseurl={http://creativecommons.org/licenses/by-nc-nd/4.0/},
	plainpages=false,			% 	- 
    colorlinks   = true, %Colours links instead of ugly boxes
    urlcolor     =  blue!50!black, %Colour for external hyperlinks
    linkcolor    = blue, %Colour of internal links
    citecolor   = green!50!black, %Colour of citations
    linktoc=page,
  	pdfborder={0 0 0},			% 	-
	breaklinks=true,			% 	- allow line break inside links
 %  bookmarks=true,
	bookmarksnumbered=true,		%
	bookmarksopenlevel=2,
	bookmarksopen=true,		%
    bookmarksdepth=3,
	final=true	% = true, nur bei web-Dokument!! (wichtig!!)
}

\begin{document}
 \title{khjxdkd kd}
 \maketitle
 
 \twocolumn[\section{hjkdgdkh kihg digdi dgidgid duihd idid}]
 zgdb uzdg dzug dug duzg iduzdg izdg udfuzdg dgzigdidzg idzg duzigd id iol
 zdguz wjufd tfvdzjkzgwfztscjh xczgdb czg zdgbjg ukdgikzgdb jfgkz gdizgdk j
 jzgd kzujufgkuzdg xcgf cjvzsdg dxcgvjdg kzug kzsdv ikdzgzidgildcg kcgfoicl
  duzfc cuf zdf sufzxtfgsdt soxfg 7odxt idg zuifgi7sdtos7g fkxvtgjsxctsfousf
   uxfx tufxucfx utcfuxf zuxf ioxd odxf doxzf doifd ouixf ozidf zid fio
\end{document}


